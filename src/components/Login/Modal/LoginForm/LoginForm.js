import React from 'react';
import { withFormik } from 'formik';

const FormDetail = ({
    values,
    errors,
    handleChange,
    setFieldValue,
    handleBlur,
    handleSubmit,
    isSubmitting
}) => {

}

const LoginForm = withFormik({

})