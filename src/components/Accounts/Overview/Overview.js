import React from 'react';
import { Grid } from 'semantic-ui-react';

export default () => {
  return (
    <Grid columns = {2} divided>
        <Grid.Column>
                h
        </Grid.Column>
        <Grid.Column>
                h
        </Grid.Column>
    </Grid>
  )
};
